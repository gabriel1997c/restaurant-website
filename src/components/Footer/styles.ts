export const styles = {
  outerContainer: {
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
    px: 12,
    py: 1,
  },
  footerBar: (theme) => ({
    backgroundColor: theme.palette.silver.lighter,
    height: 'auto',
  }),
  navLinksContainer: {
    justifyContent: 'flex-end',
    alignItems: 'flex-end',
  },
};
